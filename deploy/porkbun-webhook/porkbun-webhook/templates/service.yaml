apiVersion: v1
kind: Service
metadata:
  name: {{ include "porkbun-webhook.fullname" . }}
  labels:
    {{- include "porkbun-webhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "porkbun-webhook.selectorLabels" . | nindent 4 }}
