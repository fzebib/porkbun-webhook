apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "porkbun-webhook.fullname" . }}-test-connection"
  labels:
    {{- include "porkbun-webhook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "porkbun-webhook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
